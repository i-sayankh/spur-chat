{
  "name": "spur-chat",
  "version": "1.0.0",
  "private": true,
  "description": "Spur — AI live chat agent for a fictional e-commerce store",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace shared",
    "build:server": "npm run build:shared && npm run build --workspace @spur-chat/server",
    "build:web": "npm run build:shared && npm run build --workspace @spur-chat/web",
    "build": "npm run build --workspaces --if-present",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "prisma:migrate": "npm run prisma:migrate --workspace server",
    "prisma:deploy": "npm run prisma:deploy --workspace server",
    "prisma:seed": "npm run prisma:seed --workspace server"
  },
  "engines": {
    "node": ">=20"
  }
}
